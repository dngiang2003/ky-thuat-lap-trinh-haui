#include <iostream>
#include <math.h>
#include <string>

using namespace std;

int main() {
	string s1, s2;
	fflush(stdin);
	getline(cin, s1);
	getline(cin, s2);
	int a;
	a = (int)s1;
//	cout << (int) s1 << " " << (int) s2;
//	return 0;
}

